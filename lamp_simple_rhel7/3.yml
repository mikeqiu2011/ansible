---

  - name: test json filter
    hosts: web
    gather_facts: false

    tasks:
    - name: test 
      shell: cat /tmp/test.json
      register: result

    - name: show result
      debug:
        msg: "{{result.stdout | json_query('domain.cluster[*].name')}}"

